# Phase 3. 모노레포 및 BFF 구성 Task

## 1. Workspace 루트 전환

- [x] `pnpm-workspace.yaml`을 추가해 workspace 패키지 범위를 정의한다.
- [x] 루트 `package.json`을 workspace 기준 스크립트 구조로 정리한다.
- [x] 루트 `tsconfig` 구조를 web, bff, contracts가 재사용할 수 있게 정리한다.
- [x] 루트 `.gitignore`에 모노레포 전환 후 필요한 제외 규칙이 누락되지 않았는지 점검한다.

## 2. 패키지 디렉터리 준비

- [x] `apps/web` 디렉터리를 생성한다.
- [x] `apps/bff` 디렉터리를 생성한다.
- [x] `packages/contracts` 디렉터리를 생성한다.
- [x] 각 패키지에 기본 `package.json`을 추가한다.

## 3. 웹 앱 이동 준비

- [x] 현재 웹 앱 파일 중 `apps/web`로 이동할 대상 범위를 확정한다.
- [x] `apps/web`에 Vite 앱 엔트리 파일을 배치한다.
- [x] `apps/web`에 기존 `src` 구조를 옮긴다.
- [x] `apps/web` 기준으로 정적 자산과 앱 설정 파일 위치를 정리한다.
- [x] 웹 앱이 새 패키지 경로에서 타입 체크와 빌드를 통과하도록 경로를 정리한다.

## 4. 웹 앱 workspace 연결

- [x] `apps/web`의 `package.json`에 기존 웹 앱 의존성과 스크립트를 옮긴다.
- [x] `apps/web` 기준 `tsconfig`를 정리한다.
- [x] `apps/web` 기준 Vite 설정과 경로 별칭을 정리한다.
- [x] 루트 실행 스크립트가 `apps/web` 개발 서버를 실행할 수 있게 연결한다.

## 5. 웹 이관 후 루트 정리

- [x] 루트에 남아 있는 웹 전이용 설정 파일을 정리한다.
- [x] 루트와 `apps/web`의 빌드 산출물 위치를 정리한다.
- [x] `tsconfig`를 `base`, `web`, `server` 계층으로 재정리한다.
- [x] web과 bff가 공통 TypeScript 컨벤션을 공유하되 환경별 설정은 분리되도록 기준을 맞춘다.

## 6. BFF 패키지 기본 설정

- [x] `apps/bff`에 Fastify와 TypeScript 기본 의존성을 추가한다.
- [x] `apps/bff`가 공통 `base`와 `server` 설정을 확장하는 `tsconfig`를 추가한다.
- [x] `apps/bff` 서버 엔트리 파일을 생성한다.
- [x] `apps/bff` 개발 서버와 빌드 스크립트를 설정한다.

## 7. BFF 기본 서버 구조

- [x] Fastify 인스턴스 생성 파일을 분리한다.
- [x] 서버 시작 파일과 앱 생성 책임을 분리한다.
- [x] 최소 헬스체크 라우트를 추가한다.
- [x] BFF 공통 라우트 등록 구조를 만든다.

## 8. BFF 환경변수와 설정

- [x] BFF 전용 `.env.example` 항목을 정의한다.
- [x] Open API 인증키와 base URL을 읽는 설정 모듈을 만든다.
- [x] 필수 서버 환경변수 누락 시 실패하는 방어 로직을 추가한다.
- [x] 웹 앱 공개 env와 BFF 시크릿 env 경계가 섞이지 않게 문서와 설정을 맞춘다.

## 9. 공통 계약 패키지 시작점 구성

- [x] `packages/contracts` 공개 엔트리 파일을 추가한다.
- [x] 공통 식별자 타입 초안을 추가한다.
- [x] 공통 에러 응답 타입 초안을 추가한다.
- [x] 지역 코드와 세부 지역 코드 공통 스키마를 둘 위치를 정리한다.

## 10. BFF 내부 계약 초안 정의

- [x] 도서 검색 응답용 내부 계약 타입을 정의한다.
- [x] 도서 상세 응답용 내부 계약 타입을 정의한다.
- [x] 도서관 조회 응답용 내부 계약 타입을 정의한다.
- [x] 웹 앱 UI가 사용할 모델 타입과 raw 응답 타입의 경계를 문서 기준으로 맞춘다.

## 11. 외부 Open API 요청 기반 준비

- [x] BFF에서 외부 Open API를 호출할 공통 fetch 래퍼 위치를 정한다.
- [x] 모든 외부 요청에 `authKey`, `format=json`을 포함하는 기준을 구현한다.
- [x] 빈 검색 조건 파라미터를 쿼리에서 제외하는 규칙을 구현한다.
- [x] 필수 파라미터가 없을 때 호출하지 않는 가드를 구현한다.

## 12. 외부 응답 파싱 유틸 준비

- [x] `response` 루트 접근을 공통 처리하는 파싱 기준을 만든다.
- [x] `docs[].doc` 형태를 안전하게 읽는 로직을 추가한다.
- [x] `libs[].lib` 형태를 안전하게 읽는 로직을 추가한다.
- [x] 빈 문자열과 누락 필드를 후처리하는 기준을 추가한다.

## 13. 도서 검색 BFF 엔드포인트

- [x] 도서 검색 라우트 경로를 정의한다.
- [x] 검색 요청 파라미터를 검증한다.
- [x] 외부 `srchBooks` API 호출을 연결한다.
- [x] 외부 응답을 내부 계약으로 정규화한다.
- [x] 검색 결과 0건을 빈 상태 응답으로 구분한다.

## 14. BFF 도서 API 공통 리팩터링

- [x] 도서 검색 전용 검증과 정규화 로직을 라우트 파일 하나로 다시 통합한다.
- [x] 도서 API 입력 검증을 `zod` 스키마 기반으로 전환한다.
- [x] 재사용 가능한 도서 API 입력 스키마를 `src/schemas` 위치로 정리한다.
- [x] 응답 루트 파싱과 값 정규화 공통 유틸을 `src/utils`로 정리한다.
- [x] 공통 에러 응답 생성 함수를 `src/utils`로 정리한다.
- [x] BFF 라우트가 `packages/contracts`의 응답 타입과 에러 타입을 직접 사용하도록 중복 타입을 제거한다.
- [x] 도서 상세와 도서관 조회가 같은 `schemas`, `utils` 경계를 재사용할 수 있게 정리한다.

## 15. 도서 상세 BFF 엔드포인트

- [x] 도서 상세 라우트 경로를 정의한다.
- [x] `isbn13` 필수 검증을 추가한다.
- [x] 외부 `srchDtlList` API 호출을 연결한다.
- [x] 상세 정보와 대출 정보를 내부 계약으로 정규화한다.
- [x] 누락 필드와 부분 응답을 안전하게 처리한다.

## 16. 도서관 조회 BFF 엔드포인트

- [x] 도서관 조회 라우트 경로를 정의한다.
- [x] `isbn`, `region` 필수 검증을 추가한다.
- [x] 외부 `libSrchByBook` API 호출을 연결한다.
- [x] 도서관 목록을 내부 계약으로 정규화한다.
- [x] `dtl_region` 선택 파라미터를 조건부로 포함한다.

## 17. BFF 에러 응답 정규화

- [ ] 외부 API 호출 실패를 공통 서버 에러 구조로 변환한다.
- [ ] 재시도 가능한 에러 메시지 기준을 정의한다.
- [ ] 필수 파라미터 누락 시 클라이언트 오류 응답을 분리한다.
- [ ] 웹 앱이 빈 상태와 오류 상태를 구분할 수 있는 응답 형태를 정리한다.

## 18. 웹 앱 요청 경계 전환

- [ ] 웹 앱에서 사용할 BFF base URL 접근 방식을 정리한다.
- [ ] 웹 앱이 외부 Open API가 아닌 BFF 엔드포인트를 호출하도록 기준을 반영한다.
- [ ] 웹 앱 코드에 외부 Open API base URL 또는 auth key 접근이 없는지 점검한다.
- [ ] 이후 entities가 사용할 API 경계 문구를 문서에 반영한다.

## 19. 개발 스크립트와 실행 흐름

- [ ] 루트에서 web과 bff를 함께 실행하는 스크립트를 추가한다.
- [ ] web만 실행하는 스크립트를 추가한다.
- [ ] bff만 실행하는 스크립트를 추가한다.
- [ ] 루트에서 전체 빌드를 실행하는 스크립트를 추가한다.

## 20. 검증과 테스트

- [ ] `packages/contracts` 타입이 web과 bff에서 함께 해석되는지 확인한다.
- [ ] BFF 헬스체크 또는 샘플 라우트 테스트를 추가한다.
- [ ] 필수 파라미터 누락 시 외부 호출 없이 실패하는 테스트를 추가한다.
- [ ] 0건 응답과 실패 응답 정규화 테스트를 추가한다.
- [ ] web, bff, contracts 기준 타입 체크와 빌드를 모두 통과시킨다.

## 21. 문서 정리

- [ ] Phase 3 `spec.md`와 실제 구현 결과 차이를 반영한다.
- [ ] 루트 `README.md`를 모노레포 실행 방식에 맞게 갱신한다.
- [ ] 루트 `AGENTS.md`에 web, bff, contracts 작업 규칙이 더 필요하면 보완한다.
- [ ] 루트 `plan.md`에 Phase 3 완료 상태를 반영한다.
